# docker-compose.yml

version: "3.9"

services:
  node-app:
    image: ${DOCKERHUB_USER}/tws_app:${DOCKER_TAG}
    container_name: TWS-APP
    ports:
      - "3000:3000"
    restart: always


















# version: "3.9"

# services:
#   node-app:
#     build: .
#     container_name: TWS-APP
#     ports:
#       - "3000:3000"
#     restart: always
#     environment:
#       - NODE_ENV=production
#     volumes:
#       - .:/app
#       - /app/node_modules
